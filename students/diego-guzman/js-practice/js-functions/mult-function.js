let numOne = parseInt(prompt('Enter a number'));
let numTwo = parseInt(prompt('Enter a number'));

function multiply (numOne, numTwo){
    return numOne*numTwo;
}
document.write(`<h1>${multiply(numOne,numTwo)}</h1>`);

