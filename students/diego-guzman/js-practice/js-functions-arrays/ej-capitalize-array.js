// Crear una función que reciba un string y devuelva el string en mayusculas

let myStr = prompt('Enter a string');

function allCapital(str){
    return str.toUpperCase()}

document.write(allCapital(myStr))